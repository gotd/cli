name: goreleaser

on:
  push:
    tags:
      - '*'

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
        with:
          fetch-depth: 0
      - name: Set up Go
        uses: actions/setup-go@v6.4.0
        with:
          go-version: "1.25"

      - name: Get Go environment
        id: go-env
        run: |
          echo "::set-output name=cache::$(go env GOCACHE)"
          echo "::set-output name=modcache::$(go env GOMODCACHE)"

      - name: Set up cache
        uses: actions/cache@v4.2.3
        with:
          path: |
            ${{ steps.go-env.outputs.cache }}
            ${{ steps.go-env.outputs.modcache }}
          key: release-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            release-go-

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v7
        with:
          distribution: goreleaser
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Built-in app credentials injected at build time; fall back to the
          # public tdesktop app when the secrets are not configured (an unset
          # secret expands to an empty string, so default here with ||).
          APP_ID: ${{ secrets.APP_ID || '2040' }}
          APP_HASH: ${{ secrets.APP_HASH || 'b18441a1ff607e10a989891a5462e627' }}
      - name: Upload assets
        uses: actions/upload-artifact@v4
        with:
          name: cli
          path: dist/*